import { EnvironmentConstants } from './environment-constants';

describe('EnvironmentConstants', () => {
  it('should create an instance', () => {
    expect(new EnvironmentConstants()).toBeTruthy();
  });
});
